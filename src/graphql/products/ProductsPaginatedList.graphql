query ProductsPaginatedList(
	$take: Int!
	$skip: Int
	$orderBy: ProductSortBy
	$order: SortDirection
) {
	products(take: $take, skip: $skip, orderBy: $orderBy, order: $order) {
		data {
			...ProductsListItem
		}
		meta {
			total
		}
	}
}
