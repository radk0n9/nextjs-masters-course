mutation CartFindOrCreate($id: ID!) {
	cartFindOrCreate(id: $id, input: {}) {
		id
		items {
			quantity
			product {
				id
				name
				price
				categories {
					slug
					name
				}
				images {
					url
					alt
					height
					width
				}
			}
		}
	}
}
